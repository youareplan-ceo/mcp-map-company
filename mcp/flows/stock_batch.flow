name: stock_batch
steps:
  - tool: portfolio
    action: batch_sma
    args:
      tickers: ["AAPL","MSFT","NVDA","AMZN","GOOGL","005930.KS","000660.KS"]
      period: "3mo"
      interval: "1d"
      fast: 5
      slow: 20

  - gate: manual_approval

  - agent: notifier
    task: summary_to_slack
    args:
      text: |
        📊 StockPilot 배치 결과 ({{ portfolio.count }} 종목)
        {% for r in portfolio.results[:10] %}
        - {{ r.ticker }} | 종가: {{ r.last_close }} | 5MA: {{ r.fast_ma }} | 20MA: {{ r.slow_ma }} | 신호: {{ r.signal }} {{ r.crossed if r.crossed else "" }}
        {% endfor %}
