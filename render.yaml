services:
  - type: web
    name: mcp-map-company
    runtime: python
    buildCommand: bash -lc 'PIP_REQUIRE_HASHES= pip install -r requirements.txt'
    startCommand: python -m uvicorn mcp.run:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
