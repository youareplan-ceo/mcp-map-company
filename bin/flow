#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PY="$ROOT/.venv/bin/python"
if [[ -x "$PY" ]]; then
  exec "$PY" -m mcp.run "$@"
else
  # 비상시(venv 없을 때) 시스템 파이썬 시도
  exec python3 -m mcp.run "$@"
fi
